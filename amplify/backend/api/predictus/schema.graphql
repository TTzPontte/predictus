type Report @model @auth(rules: [{ allow: public }]) {
  id: ID!
  link: String
  status: String
  fileName: String
}

type Users @model @auth(rules: [{ allow: public }]) {
  id: ID!
}

type Mutation {
  runOCR: String @function(name: "ocrnodefn-${env}")
}
